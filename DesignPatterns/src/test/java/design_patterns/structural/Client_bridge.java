package design_patterns.structural;

public class Client_bridge {
    public static void main(String[] args) {
        /*Our implementations and abstractions are generally coupled to each other in normal inheritance.
        * Using Bridge pattern we can decouple them so that both change without affecting each other.*/

        /*We achieve this feat by creating two separate inheritance hierarchies; one for implementation and another for abstraction.
        * We use composition to bridge these two hierarchies*/

        /*wat is bridge pattern?
        * Use this when we want to decouple abstraction from its implementation.
        *
        *
        * */

    }
}
